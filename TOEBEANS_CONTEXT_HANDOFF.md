CONTEXT HANDOFF — Toebeans Project
Paste this at the start of any new chat with Claude (advisor role). For Claude Code sessions, don't paste this — the repo's CLAUDE.md and ROADMAP.md are its context.

What this project is

* Toebeans — cozy low-poly 3D sim, casual multiplayer (phased in later)
* Free browser version (ad revenue via web portals like Poki/CrazyGames) markets a paid Steam version (co-op, extra content, achievements)
* Owner is non-technical and is the game director: all creative decisions (design, art approval, scope calls, what's fun) are theirs. Claude handles all technical execution. If Claude drifts into designing, the correction is: "Director call — give me 2–4 options with tradeoffs, I'll choose."

Locked decisions (do not relitigate or contradict)

* Stack: TypeScript strict + Three.js + Vite. Repo layout: /client /server /shared /assets. All game logic = pure functions in /shared on a serializable GameState; rendering never mutates state (multiplayer-proofing).
* Multiplayer is phased: single-player v1.0 first → async social (visits/gifting) as M6 → real-time co-op as the Steam headline feature (M7). Not built yet. Friend/cat/environment viewing is deferred to M6 (director decision, July 20, 2026) — v1.0 is strictly single-player.
* Steam path: Electron wrapper + steamworks.js, later. Steam store page goes live at web launch (M5) to accumulate wishlists. $100 Steam Direct fee.
* Art: low poly, .glb only, CC0 packs first, AI-gen assets only if matching the written style bible; every asset licensed in assets/CREDITS.md; props ≤2k tris. The Art Style Bible IS written (DESIGN.md, July 21, 2026, from the director's five Omno reference images): 12-color palette, shape language, lighting/haze rules, snow-remembers rules. Visual target: Omno.
* Workflow: no pull requests, ever — solo project, director reviews and approves in-session, Claude Code commits and merges straight to master. Confirmed as a standing convention (ROADMAP's M1 ski-slope entry explicitly logs a direct merge, no PR).
* Dev tool: Claude Code in the desktop app (repo lives on the owner's PC). Owner never touches a command line; anything involving accounts/credentials/payments is done by the owner clicking, with Claude preparing everything up to that point. Cloud sessions (Claude Code on the web) are also in use when the owner is away from the PC — they work directly off GitHub and commit straight to master; the local PC copy must `git pull` at the start of the next desktop session whenever a cloud session has landed work.
* Process: milestones M0 setup → M1 gray-box prototype (fun check gate) → M2 vertical slice → M3 content → M4 polish (60fps mid laptop, <15MB initial load) → M5 web launch (itch.io first, then portals) → M6 async social → M7 Steam. One feature per Claude Code session; ROADMAP.md updated every session; new ideas go to IDEAS.md, reviewed only between milestones.
* Repo access: the repo is currently PUBLIC (flipped from private on July 20, 2026, specifically so advisor Claude could read files directly instead of the owner pasting them in). Advisor Claude can now fetch CLAUDE.md, DESIGN.md, IDEAS.md, README.md, and ROADMAP.md straight from GitHub — no copy-paste needed, though the owner may still paste things ad hoc (e.g. the Director's Playbook, which lives as a Claude.ai artifact and is NOT yet committed to the repo).

Key documents the owner has

1. DIRECTORS_PLAYBOOK.md — step-by-step process doc: every step is "You decide/You do" or "Ask Claude: [copy-paste prompt]." Exists as a Claude.ai artifact only; not yet in the repo.
2. Repo files: CLAUDE.md (conventions), ROADMAP.md (session log), TOEBEANS_VISION.md (director's own vision doc — the source of truth for design if it and DESIGN.md ever disagree), DESIGN.md (working design doc restating the vision, plus the approved v1.0/v1.x/Steam scope split and the Art Style Bible), IDEAS.md (parked ideas), README.md, assets/CREDITS.md (per-asset license ledger), TOEBEANS_CONTEXT_HANDOFF.md (this file).

What the game is (from DESIGN.md — decided, do not contradict)

* Core fantasy: you're a human with a pet cat (both customizable); you live in a small environment (starting as a boring bedroom) that you decorate; the cat is your best friend — pet it, pick it up, hug it.
* Core loop: ski mountain slopes (cat strapped to your back) → earn XP → level up → unlock environments/furniture/appliances/cosmetics → decorate and interact for more XP → repeat.
* Skiing: controls are left/right/up(speed up)/down(brake)/jump/crouch/boost; hazards = chasing snowballs, chasms, tree limbs (crouch is for these — not built yet); checkpoints on crash; the cat's 9 lives — lose all and the run forfeits for half XP. v1.0 slopes are handcrafted (3 of them); random-gen difficulty modes may come later. Faster finish = more XP.
* Progression: no stat upgrades ever, identical base stats for all; leveling unlocks environments (bedroom → apartment → skyrise → space shuttle, laboratory, Mars, Heaven, jungle…), furniture/customization, and cosmetics. Environments are collected, not replaced; each saves its own layout.
* Environment XP: timed-task XP from appliances/tech (grill, TV, computer, reading); passive/AFK XP from furniture (bed, couch), accruing up to 24h while the game is closed, calculated on return.
* Multiplayer (M6+, not v1.0): head-to-head slope races, leaderboards, friend visits with a 15% XP boost for both, cats socializing. Full details in DESIGN.md.
* Look & feel: low-poly cute wholesome; Sims-style rotating bird's-eye camera in environments, 2.5D isometric/three-quarter side-scroller for skiing; LOFI music + ambient audio; small detail touches (ski trails, glowing lamps, crackling fireplaces, meows).
* Design intent: cozy = comfort, a relaxing second home away from the world's stress; progression is open-ended with no fixed endgame; differentiation from Stardew/Animal Crossing comes from the graphics (Omno target) and gameplay.

v1.0 scope (locked, approved by director July 20, 2026 — see DESIGN.md "Scope: v1.0 / v1.x / Steam")

* One environment (the bedroom), 6–8 starter furniture/appliance items (at least one timed-task + one passive/AFK), furniture placement system, 3 handcrafted ski slopes with full hazard set + checkpoints + 9-lives, XP/leveling with level-gated unlocks, browser-storage save system, basic character + cat customization, music/ambient audio hookup.
* Everything else (more environments/slopes, procedural difficulty modes, environment save slots, expanded catalog, M6 async social) is v1.x. Real-time co-op, leaderboards, friend visits, and achievements are Steam-phase only.

Current status (as of July 22, 2026)

* Phase 0 (setup): DONE. Scaffold built and running. Repo `toebeans` on GitHub — now PUBLIC (was private at creation).
* Phase 1, Steps 1.1–1.4 (design + scope split): DONE. TOEBEANS_VISION.md written by the director; DESIGN.md scribed from it; three open design questions resolved; v1.0/v1.x/Steam cut approved as proposed. Friend/cat/environment viewing deferred to M6.
* Phase 1, Step 1.5 (art references → style bible): DONE (July 21, 2026). Director supplied five Omno reference images with likes/dislikes; the Art Style Bible was written into DESIGN.md: 12-color palette, shape language, lighting/haze rules, "snow remembers" rules (ski trails/footprints — the director's one explicit dislike in the references was flat depthless snow), asset sourcing rules, per-asset triangle budgets, and the CREDITS.md requirement.
* Phase 2 / M1 (gray-box prototype): COMPLETE, July 21, 2026. All build items done (gray-box slope with 3 chasms; 9 lives + crash/checkpoint loop; gray-box bedroom walk; cat follows/sits, including a route-around-furniture fix after playtest feedback). Fun check gate: director playtested and ruled **PASS, barely** — the loop justifies investing in art, but the thin margin means feel tuning (speeds, jump arc, crash pause, follow distances) stays a live concern through M2 rather than a solved problem.
* Phase 3 / M2 (vertical slice — polish ONE area end to end): IN PROGRESS.
  - Director call (July 21, 2026): the polish area is the **ski slope**; the bedroom reaches the same level in M3.
  - Director call (same day): assets from the **Quaternius Ultimate Nature Pack** (CC0, chosen from a researched shortlist).
  - [x] First real assets: 24 snow-variant models (birches, dead birches, pines, rocks, stump, log, bushes) converted to .glb by a new repo tool (`tools/obj2glb_palette.py`) that remaps every material to the bible's 12-color palette, snaps origins to base, and enforces the 2k-triangle prop budget. All 24 credited in assets/CREDITS.md. 87 instances scattered seeded-deterministically along the slope flanks; scene colors (sky, snow, chasms, checkpoints, skier blue, cat amber) aligned to the palette in both scenes.
  - Lighting/haze pass: DONE (July 21, 2026). Director playtested it: "starting to come together" — approved as-is, with picky visual tweaks deliberately deferred until all M2 items are done (one tuning pass at the end, not nibbling between features). One low warm sun with soft blue shadows (shadow color derived mathematically from the palette's two snow colors, verified by pixel-reading the rendered frame: shadowed snow lands within 1/255 of palette #2), dawn-pink distance fog, a gradient sky dome, and a visible glowing sun disc just above the horizon. Note: Three.js retired its "PCFSoft" shadow mode in r185; the bible's parenthetical was updated (soft edges now come from PCF + blur radius — same look, different knob).
  - [x] Real UI: DONE (July 21, 2026). Plain-text HUD replaced with a palette-styled DOM overlay (client/src/hud.ts): the cat's 9 lives as nine cat-face icons that fade to snow-shadow blue when spent, centered crash/forfeit banners (forfeit is the game's one signal-red panel), and keycap-chip control hints per scene. Direction defaults chosen in-session (questions went unanswered): HUD-only scope (no title screen — parked in IDEAS.md), 9 icons over icon-×-number, soft-rounded tone over Omno-minimal. A bundled rounded font (Fredoka/Baloo) is parked in IDEAS.md pending a director yes/no (requires a file download).
  - Slope sound EFFECTS: DONE (July 21, 2026) — synthesized in-browser with Web Audio (no audio files/licenses): speed-tracking wind + ski-carve hiss (silent mid-air), boost rush, and one-shots for jump/land/crash/checkpoint/respawn/forfeit; M mutes. Playtest verdict (same day): PASSED — "speed feels real now," wind is the director's favorite; one note, the carve hiss is too loud → parked in IDEAS.md for the end-of-M2 tuning pass. Music direction CALLED (July 21, 2026): each slope gets a timed song synced to its layout, Geometry Dash style — tense before big jumps (details in IDEAS.md; supersedes the lofi/ambient/instrumental question for the slope) — and music is deliberately the LAST M2 item, built after everything else.
  - Direction questions re-asked and answered (July 21, 2026): UI defaults — nine cat-face lives and HUD-only scope RATIFIED; visual tone CHANGED to a middle ground (cat faces stay cute, pills/chunk/panels calm down — restyle is its own upcoming session). Title screen idea evolved: it should be a *dynamic* showcase of the world (landscape, enemies, terrain, animals) as those get built — parked in IDEAS.md.
  - [x] UI tone restyle: DONE (July 21, 2026). The HUD moved from soft-rounded to the director's middle ground — panels are now soft rounded rectangles (10–12px) instead of full pills, borders are 1px hairlines in snow-shadow blue, backgrounds slightly more translucent, banner/hint lettering dropped from extra-bold to semi-bold with more letter-spacing, and the keycap chips lost their thick 3D bottom edge. The cat-face life icons are untouched (director: they stay cute). CSS-only change in client/src/hud.ts.
  - [x] Save/load: DONE and PLAYTEST-PASSED (July 21, 2026; director verdict: "the saves are working perfectly," shipped as-is). Browser-storage persistence. A save is a JSON snapshot of only the *dynamic* game state (player/cat positions, ski run distance/lives/status/checkpoint, mute setting); the *static* layout (room, furniture, chasms, checkpoints) always reloads from the `createInitial*` code, so retuning the slope later never traps a stale layout inside a save. Pure save logic lives in shared/src/save.ts (10 tests); the browser-storage glue is client/src/save.ts. Loading is strict (rejects corrupt/old-version/impossible saves → clean fresh start) and self-healing (clamps out-of-range positions, snaps a vanished checkpoint down to the nearest one passed). Saves on scene switch, mute, a 5s autosave, and tab-hide/close.
  - [x] The CAT is a real model: DONE (July 21, 2026). Asset research first, per the director: no CC0 skiing *human* exists anywhere (only CC-BY or paid), but a CC0 rigged, animated cat by Quaternius does — https://poly.pizza/m/qKICY6xla2 — same artist as the Nature Pack our trees came from, so it matches by construction. It arrived colored by a shared texture atlas, which the Art Style Bible bans, so a new tool (tools/glb_palette.py) bakes each vertex's atlas swatch into palette vertex colors and strips the texture: body → birch amber, belly → birch bark, eyes → deep slate, nose → signal red (2,448 tris, inside the ~5k character budget). client/src/catModel.ts owns the model for both scenes — it normalizes the model to 0.42 units tall with paws on the ground, cross-fades clips, and adds the code-built signal-red scarf that ties the animal to the HUD's cat faces. The bedroom cat's existing sitting/following moods now drive the Idle/Walk clips; on the slope the cat is parented to the skier so it tips over on a crash. No /shared changes; 38 tests unchanged.
  - [x] SKIER model + character customization colors (July 21, 2026). Three director calls: (1) skin/hair get a SEPARATE character palette, leaving the landscape 12 untouched — 8 skin tones, 8 hair colors, 5 eyes, 5 coats, 4 trousers, 3 boots, and signal red deliberately in none of them; (2) customization depth = colors + a few hairstyles, of which COLORS shipped and hairstyle geometry is parked as its own session; (3) base model = BUILD BOTH, director picks at playtest. The Animated Character Pack's male model turned out unusable (one material, no color separation, renders monochrome), so the two shipped candidates are `Skier_Modular.glb` (six named materials, 1,852 tris) and `Skier_Animated.glb` (atlas baked to palette vertex colors, 1,908 tris). Press B in the bedroom to swap; K and H cycle skin and hair. Appearance lives in `shared/src/appearance.ts` as INDICES into the ramps (can't drift off-palette) and rides in the save — SAVE_VERSION 1 → 2, so pre-existing saves are discarded. Two bugs caught by verification and fixed: the skier leaned uphill (lean was applied inside the downhill half-turn), and the cat rode inside the skier's chest (offset was tuned against the old 1-unit box).
  - Skier PLAYTEST VERDICT (July 21, 2026): needed another pass. Five issues: (1) no slope animation (standing idle — no CC0 skiing clip exists); (2) no skis; (3) cat faces the camera, should face downhill; (4) bedroom walking is jagged (8-way heading snaps); (5) **the characters don't match the art style** — both bases were realistic ~6-heads-tall humans, failing the bible's "cute lives in the characters" rule next to the chunky cat. Director call: a CHARACTER PASS as its own session; the question became "what does a Toebeans human look like", not "which of these two".
  - [x] The CHARACTER PASS: DONE (July 21, 2026). Director directive: use Quaternius's **Ultimate Animated Character Pack** (50 CC0 characters, chunky/big-headed/cute, same artist as the cat and scenery) — "let players select from these." So the customization model changed: instead of dressing one body, the player **picks a character** from a curated roster and tints its **skin and hair**; the outfit comes baked-in with the character. `shared/src/appearance.ts` went from six color regions on one body to `{character, skin, hair}` (indices into a roster + the skin/hair ramps). Starter roster = 11 cozy characters (Casual ×7, OldClassy ×2, Cowboy ×2); the pack's costume characters (knights/ninjas/etc.) are parked in IDEAS.md as XP-unlock candidates. New tool `tools/gltf_character.py` (the pack is textureless with named materials, so recolor = rewrite baseColorFactor, no baking; it also exploits the pack's single shared skeleton — one shared `CharacterClips.glb` bound to any character by bone name, so 11 characters + clips ≈ 5 MB not ~20). `client/src/skierModel.ts` rewritten to load a character + shared clips and scale off the Head bone (hats add height, don't shrink the body). Both old `Skier_*.glb` bases deleted. `SAVE_VERSION` 2 → 3 (old saves discarded). Temporary keys: **C** cycles character, K/H cycle skin/hair — stand-ins for the M3 picker UI. `npm run check` (55 tests) and `npm run build` pass; verified by pixel-reading rendered frames — every character renders feet-on-ground at 1.56–1.64 units (hats push OldClassy to 1.83, Cowboy to 1.72), scaling linear, skin/hair recolor to exact palette hexes, outfit baked palette-correct, and the running game cycles/persists/scene-switches with no errors. A correction surfaced while checking: the model the repo had called `Skier_Animated.glb` was NOT from this pack (41-bone Mixamo rig from Poly Pizza) — CREDITS.md is fixed. NB: the pack's site only lists FBX/OBJ/Blend, but the download's Google Drive has a glTF subfolder, which the tool consumes.
  - Character-pass PLAYTEST VERDICT (July 21, 2026): the roster reads right, but the character ON THE SLOPE needs a polish pass. Six issues, all parked in IDEAS.md for a later session (director's call): (1) no feet — the pack characters have no shoe/boot geometry (bare leg-stumps) and feet likely sink into the snow; (2) character can be changed while skiing — the C/K/H keys aren't gated to the bedroom (one-line fix in main.ts); (3) hair doesn't move — it's part of the skinned mesh, so verify head-bone weighting else it's just the low-poly no-secondary-motion look; (4) no ski equipment (already the next session's work); (5) character stands straight while skiing — the fixed forward lean is too subtle and the up/down lean INPUT produces no visible body change, wants a real ski pose driven by the lean; (6) cat sits halfway in the character's hair — the back-mount (0,0.95,0.16) was tuned to the old bodies and lands at head height now, needs re-tuning (settle with the cat-facing fix). Issues 1/4/5/6 cluster into the ski-pose session; 2 is trivial; 3 is low-priority cosmetic.
  - [x] SKI POSE + SKIS + character-on-slope polish: DONE (July 22, 2026). A real crouched ski pose built in code on the pack's shared skeleton (no CC0 skiing clip exists anywhere): bone rotation offsets over a frozen Idle base frame, blending between a *braking* pose and a *full tuck* driven by the run's speed — which fully encodes the up/down lean input, so the speed control finally reads on the body (playtest issue #5); airborne adds extra tuck. A rig quirk made it natural: the pack's foot bones are root-level IK-style bones, so dropping the pelvis folds the knees while the feet stay planted. Ski gear (skis/boots/poles) is code-built flat-shaded primitives in client/src/skierModel.ts — the boot boxes double as the "no feet" fix, the poles glue to the fist bones every frame, skis are birch amber (pale bark wood measured near-invisible against sunlit snow), and the gear only shows on the slope. The cat re-mounted to the crouched upper back at (0, 0.62, 0.30) and faced DOWNHILL (director call; the scarf is now mostly hidden from the camera — accepted cost). Ride-alongs: C/K/H gated to the bedroom, and the bedroom heading now eases (shortest way round) instead of snapping between the 8 input angles. /shared change: one additive export (MIN_SPEED). 55 tests + build pass. Live verification (ASCII silhouettes from pixel reads) caught four real bugs: GLTFLoader strips dots from bone names ("Foot.L" → "FootL", the pose was silently a no-op); the pelvis bone's name collides with a mesh node (now found structurally as Hips' parent); a three.js dirty-check optimization skips rewriting bones under a paused clip (relative offsets accumulated into a spin — the pose now overwrites bones absolutely from a captured base); and the first poles punched 0.25 units through the snow (shortened + re-angled).
  - Ski-pose PLAYTEST VERDICT (July 22, 2026): needs a ROUND 2. Liked: cat faces forward, real ski equipment exists at all, customization gate works. Eight issues, all parked in IDEAS.md with cause analysis: (1) skier never turns — steering slides sideways with no yaw/bank; (2) legs/arms not independent — the pose is symmetric and frozen, reads as a rigid block; (3) gear style doesn't match the chunky graphics (plain boxes/thin cylinders); (4) skis too short — director wants longer (the "short and cute" sizing was rejected); (5) cat floats — should HUG the character's back and peek over a shoulder (a mount redesign, not an offset tweak); (6) hair should move with real physics; (7) hair should react physically against the cat (still overlaps); (8) character still reads as footless — boots are slope-only gear, bedroom still shows bare stumps; wants always-on code-built shoes attached to the Foot bones in both scenes. The through-line: the pose works, the LIFE is missing.
  - [x] Round-2 chunk 1 — MOTION & LIFE: DONE (July 22, 2026; director's pick from the eight-issue list — it attacks the "rigid block" headline). Issues (1) and (2) resolved: a new carve layer in the skier rig yaws the character toward the actual movement direction (derived frame-to-frame from state.lateral, like the bedroom heading) and rolls the body into a carving bank, eased so swerves flow; the skis turn and tilt onto their edges with the body, and the cat's mount moved inside the carve layer so it rides the turns instead of hovering while the body yaws under it. The frozen symmetric pose is gone: staggered stance (left ski/boot/foot lead by 0.1 units, asymmetric arm/leg bends, torso twist with neck counter-turn) plus a procedural life layer — speed-scaled pelvis bob (reads as knees pumping, since the feet stay pinned to the skis), independent arm float at incommensurate frequencies, torso rock, head micro-corrections, and high-frequency snow chatter that cuts out mid-air (same reasoning as the carve hiss muting airborne). Idle stays frozen per the paused-clip gotcha. Rendering-only; 55 tests + build pass; verified by driving the real modules live — steer yaw matches the movement atan2 exactly, bank caps at 0.32 rad, respawn teleports read as zero swerve, feet showed zero drift off the skis, poles 0.000 from the fists, and a mid-carve ASCII silhouette visibly tilts with skis sweeping a diagonal.
  - Motion & life PLAYTEST VERDICT (July 22, 2026): moved, but didn't land — six issues, parked in IDEAS.md's top block with cause analysis: (1) the turn isn't fluid — the whole body banks as one plank; wants real ski ANGULATION (feet/skis push out from under the body, knees bent, torso relatively upright — the fix splits the bank between the lower body and a counter-rotated spine); (2) legs still static — differently positioned but frozen; wants random movement and spacing (the life layer deliberately skipped the leg bones and foot pins; leg wobble is cheap on this rig because the feet are separate root-level bones); (3) NO MOMENTUM — runs start at speed and speed returns instantly after nearly stopping; wants resistance plus a pole push-off to get going — flagged as a /shared gameplay change, the FIRST since M1 (speed is currently computed directly from input each frame), touches chasm-jump timing and tests, its own session; (4) knees don't bend to jump — no takeoff extension or landing absorb (cheap presentation fix keyed off airborne transitions; true pre-jump anticipation would need sim-side wind-up, a fairness call); (5) ski boots are blocky — folded into the parked gear-style pass with boots specifically called out; (6) still no feet in the bedroom — always-on-feet item re-confirmed.
  - Turn angulation + leg life: BUILT July 22, 2026, playtest verdict NOT LANDED — parked by director's call (focus shifts elsewhere). What stands: skis stay ON the snow through carves (the old roll lifted the outside ski), knees pump in the life layer, stance breathes via per-side ski assemblies. What's reopened (both in IDEAS.md's top block): the turn still reads as "the ski turns the body" because no leg bone participates in a turn — round 3 must put the movement into the leg chain itself; and the feet are visibly out of the boots mid-carve (regression — boots counter-roll while feet stay level; fix is rolling the foot pins to match, verify at mesh level not center distance).
  - Cat hug + hair physics: DONE and playtested (July 22, 2026). The cat genuinely hugs the skier's back (live spine-glued mount, procedural cling pose, peeks over the right shoulder), and hair is real swinging geometry — spring-driven off head motion + wind, repelled from the cat so they never interpenetrate. Works across the roster (bald = no-op, hats don't flap). Playtest: two issues parked in IDEAS.md by director's call — hair roots detach from the scalp mid-turn (swing must fade root→tip instead of rotating the whole piece; characters read bald from some angles), and the cat's tail is stiff (wants real wind-reactive swooshing via the same spring model the hair uses).
  - [ ] Remaining M2 items, next focus is the director's pick: momentum/pole-push-off (gameplay session), jump anticipation, gear style + longer skis, always-on feet, the parked angulation round 3 + boot fix, or the hair-roots + cat-tail fixes — then music (timed per-slope songs — deliberately last), then the end-of-M2 tuning pass (parked picky tweaks + carve volume).
  - Director direction parked in IDEAS.md (July 21, 2026): wildlife on the slopes (foxes, deer, wolves) from Quaternius's Ultimate Animated Animals Pack — note that pack contains no cat; and cat customization, to ship alongside character customization.
  - Resolved by the character pass: the old t-shirt-and-shorts modular base is gone — the roster characters are fully dressed, outfits baked to palette coat colors.
  - Since July 22, 2026 the repo runs parallel Claude sessions in git worktrees (see PARALLEL.md) — restructured later the same day into THREE: **lobby** (renamed from "bedroom"), **slope-mechanics** (the sim + state wiring), and **slope-visuals** (assets, materials, rig look, audio; skiRender.ts was split so each slope session owns whole files). The slope session also widened the skiable area 3× (July 22: LATERAL_LIMIT 4 → 12, hard-clamp edge kept by director call).
  - ART DIRECTION AMENDED (July 22, 2026, direction session): the director doesn't like the flat graphics — the Art Style Bible's no-texture rule is amended. Omno stays the target for mood/lighting/palette, but surfaces get **stylized painted textures + procedural detail** (both; hand-painted feel, never photoreal; palette discipline kept). First slice is a side-by-side tree/snow texture test on the real slope for director approval; the bible's full rewrite waits on that verdict.
  - The BEDROOM ARC (July 22, 2026, all in one day): the walkable bedroom got a full room + follow camera, interior lighting (window/dawn/lamps — this fixed the old "~45% too dark" issue), real Quaternius furniture, and a rewritten collision resolver — and then the director **scrapped the whole area**: "we are scrapping the bedroom and replacing it with a more traditional lobby."
  - [x] The MENU LOBBY: DONE (July 22, 2026). The game now opens on a title screen — "Toebeans", a Hit-the-slopes button, Character/Skin/Hair/Sound buttons (keyboard shortcuts printed on them) — over a live 3D vignette: the player's character and the cat on dawn snow, slope trees framing the shot, the cat periodically strolling a half-circle and resettling. This doubles as the character-select (cycling updates the live model) and lands the long-parked title-screen framing. `shared/bedroom.ts` and `bedroomRender.ts` are deleted (git history keeps them); SAVE_VERSION 4 → 5 ("bedroom" mode became "lobby", the save's bedroom block is gone; old saves discarded). The furniture models stay in assets/bedroom/ as the future unlock pool.
  - OPEN because of the scrap (deliberately parked, director's "decide later"): where the earn-your-furniture progression loop (bare start → XP → furniture unlocks → decorate) now lives — the v1.0 decorate loop currently has no stage. Options sketched in IDEAS.md's top entry; DESIGN.md carries matching ⚠ notes. Separately, the director's "I don't like the flat graphics / no texture" challenge to the Art Style Bible STANDS and is game-wide (slope, characters, and the new lobby alike) — the art-direction session for it is still owed.
  - `crouch` is intentionally unbuilt — no hazard (tree limbs) exists yet for it to react to; will be added together with tree limbs rather than wired to nothing.
  - Claude Code's browser screenshots have timed out in every session in this repo (twelve and counting); logic gets verified by stepping the real modules in the live page or pixel-reading rendered frames, and rendered-look judgments go to the director's own eyes via `npm run dev`.

Facts to not hallucinate about

* Game design v1 direction is DECIDED and lives in DESIGN.md / TOEBEANS_VISION.md; do not contradict it or invent details beyond it.
* The v1.0/v1.x/Steam scope split IS decided (Step 1.3/1.4 both done and approved) — do not treat it as still open.
* Nothing is deployed anywhere; no itch.io/portal/Steam accounts exist yet. The game only runs via `npm run dev` on the owner's machine.
* Art assets DO exist in the repo now (24 CC0 .glb scenery models in assets/slope/, a rigged cat, 11 rigged playable characters + a shared clip file in assets/characters/, and 7 furniture models in assets/bedroom/ — currently unused in-game, held as the unlock pool — all credited in assets/CREDITS.md) and the style bible IS written (DESIGN.md), though its no-texture rule is under director review. A real HUD (cat-face lives, banners, keycap hints — restyled to the middle-ground tone), slope sound effects (synthesized, no files), save/load (browser storage, SAVE_VERSION 5), a real cat model, a pickable/customizable character roster, a real ski pose with code-built ski gear, and a menu lobby / title screen (July 22, 2026 — it replaced the walkable bedroom, which no longer exists) all exist now. XP and music still do not exist.
* The game has NO walkable home space anymore — it opens on the menu lobby. Do not describe a bedroom/room the player walks around; that was scrapped July 22, 2026, and where the decorate/furniture-unlock loop lives is an OPEN parked question.
* Multiplayer code does not exist yet.
* IDEAS.md parked items include: a finish line / run completion (needed before XP can exist), the nature pack's ~126 unused non-snow models as future-environment dressing, a bundled rounded UI font (needs a director yes/no), turning down the carve-hiss volume (end-of-M2 tuning pass), timed per-slope music à la Geometry Dash (the called music direction — built last in M2), the dynamic showcase title screen, costume characters as XP unlocks, cat customization, and slope wildlife. The TOP block in IDEAS.md is now the ski-pose playtest verdict (July 22, 2026) with eight open issues for the round-2 session: no turning/banking, rigid symmetric body, gear style mismatch, skis too short, cat should hug the back + peek over shoulder, hair physics, hair-vs-cat reaction, and always-on feet (boots are slope-only; the bedroom still shows stumps). Of the earlier character-pass six: change-character-while-skiing and no-ski-equipment stayed RESOLVED; no-feet and cat-in-hair were REOPENED by this verdict; rigid hair ESCALATED from taste call to a wanted feature (real physics). The character-mismatch and t-shirt-and-shorts items remain RESOLVED.
* README.md is current again as of July 21, 2026 (describes both scenes, the slope's first real art, and the controls).
* The repo is now public — advisor Claude can read repo files directly via GitHub's raw URLs. This does not mean advisor Claude can write to the repo; commits still go through Claude Code.
* Owner time budget: up to ~3 hrs/day. Owner is picky about graphics — visual iteration happens via screenshots pasted to Claude with short direction notes.
* An unrelated prior project called "Windowsill" exists in Claude Code's memory with a "design before build" lesson; it is not part of Toebeans.
